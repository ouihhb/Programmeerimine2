<Window x:Class="KooliProjekt.WpfApplication.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Products" Height="450" Width="900">
    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="260" />
        </Grid.ColumnDefinitions>

        <DataGrid Grid.Column="0"
                  ItemsSource="{Binding Products}"
                  SelectedItem="{Binding SelectedProduct, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  Margin="0,0,12,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="60" />
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" />
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="2*" />
                <DataGridTextColumn Header="Price" Binding="{Binding Price}" Width="100" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Column="1">
            <TextBlock Text="Id" />
            <TextBox Text="{Binding ProductId}" IsReadOnly="True" Margin="0,4,0,12" />

            <TextBlock Text="Name" />
            <TextBox Text="{Binding ProductName, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,12" />

            <TextBlock Text="Description" />
            <TextBox Text="{Binding ProductDescription, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,12" />

            <TextBlock Text="Price" />
            <TextBox Text="{Binding ProductPrice, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,18" />

            <WrapPanel>
                <Button Content="Add new" Command="{Binding AddNewCommand}" Width="76" Margin="0,0,8,8" />
                <Button Content="Save" Command="{Binding SaveCommand}" Width="76" Margin="0,0,8,8" />
                <Button Content="Delete" Command="{Binding DeleteCommand}" Width="76" Margin="0,0,0,8" />
            </WrapPanel>
        </StackPanel>
    </Grid>
</Window>
